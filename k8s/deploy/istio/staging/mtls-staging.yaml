# Enables strict mTLS for workloads in the staging namespace.
#
# Important:
# - This is correct for service-to-service traffic inside the Istio mesh.
# - Your ingress-nginx controller is not currently injected with an Istio sidecar.
#   After applying STRICT mTLS, browser traffic through ingress-nginx to staging
#   workloads may fail unless ingress traffic also enters through the mesh.
#
# Apply:
#   kubectl apply -f k8s/deploy/istio/staging/mtls-staging.yaml
#
# Roll back:
#   kubectl delete -f k8s/deploy/istio/staging/mtls-staging.yaml
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: staging-strict-mtls
  namespace: staging
spec:
  mtls:
    mode: STRICT
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: staging-istio-mutual
  namespace: staging
spec:
  host: "*.staging.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: staging-istio-mutual-from-gateway
  namespace: istio-system
spec:
  host: "*.staging.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
